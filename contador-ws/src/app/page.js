import Counter from "../components/Counter.js";

export default function Home() {
  return (
    <main className="flex flex-col items-center justify-center min-h-screen bg-primary-light text-white">
      <Counter />
    </main>
  );
}
